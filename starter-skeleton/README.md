# Starter Skeleton (Planning Artifact)

This folder is a minimal, framework-agnostic scaffold to accelerate kickoff.

## Suggested scripts (to implement next)
- `dev:web` — start frontend dev server
- `dev:api` — start backend API service
- `lint` — static checks
- `test` — unit/integration tests
- `test:e2e` — end-to-end flows
- `build` — production artifacts
- `deploy:staging` — deploy to staging

## Notes
- Keep this skeleton as reference while deciding final stack.
- Integrate with CI once stack decisions are finalized.
